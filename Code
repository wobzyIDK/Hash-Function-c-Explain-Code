#include <iostream>
#include <cstring>
//can use #include<bits/stdc++.h> for some
using namespace std;

const int OFFSET = 500000;
const int MAXN = 1000001;  
int a[MAXN];

int main() {
    int n, m;

    if (scanf("%d%d", &n, &m) != 2) return 0;

    memset(a, 0, sizeof(a));
    for(int i=0; i<n; i++) {
        int t;
        scanf("%d", &t);
        a[OFFSET + t] = 1;
    }

    for(int i = MAXN-1; i>=0 && m>0; i--) {
        if (a[i]) {
            printf("%d", i - OFFSET);
            m--;
            if(m > 0) printf(" ");  
            else printf("\n");     
        }
    }

    return 0;
}
